var player;
var stars;
var bombs;
var platforms;
var cursors;
var score = 0;
var gameOver = false;
var scoreText;
var keyR;

export class play extends Phaser.Scene {
    constructor(){
        super("play")
    }

    preload(){
        //precargar los tiles
    }
    create(){
        //crear mapa
    }
}